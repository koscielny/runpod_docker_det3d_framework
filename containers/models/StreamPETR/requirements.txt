# StreamPETR Dependencies
# Updated for compatibility with PyTorch 1.11.0 (pre-installed in Dockerfile)
# Python >= 3.8
# CUDA == 11.7 (compatible with PyTorch 1.11.0)

# OpenMMLab ecosystem (compatible versions)
mmcv-full==1.6.0
mmdet==2.28.2
mmsegmentation==0.30.0

# Optional dependencies
# flash-attn==0.2.2  # Commented out as it's optional and may cause build issues

# Other dependencies
numpy
pillow
scipy
matplotlib